<UserControl
    x:Class="UWP_TimelineTest.UserControls.FuelUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_TimelineTest.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!--<RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>-->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--<TextBlock Grid.Row="0" Grid.Column="0" Text="Date:" Margin="0,10,10,10"/>
        <CalendarDatePicker Grid.Row="0" Grid.Column="1" Margin="10" Date="{Binding EventDate, Mode=TwoWay}" Width="150"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Odometer:" Margin="0,10,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Attribute2Text, UpdateSourceTrigger=PropertyChanged}" Margin="10,10,40,10"/>-->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Volume (liter):" Margin="0,10,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FuelQuantity}" Margin="10,10,40,10"/>
    </Grid>
</UserControl>
